version: 2
updates:
  - package-ecosystem: 'terraform'
    directory: '/'
    schedule:
      interval: 'daily'
    # Raise pull requests for version updates
    # to pip against the `dev` branch
    target-branch: "dev"